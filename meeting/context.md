# Shared Context

## 目标

建立一个 AI-to-AI 协作系统：GPT 与 Doubao 能直接读取同一 GitHub 仓库中的 Markdown，通过读写文件完成连续讨论，而用户不再充当消息搬运工。

## MVP

1. 共享上下文
2. AI 独立观点
3. 回合状态
4. 共识与任务沉淀
5. 后续再接自动触发器/API

## 当前核心问题

GitHub 只是共享黑板，本身不会自动让两个模型互相调用。因此最终系统需要在黑板协议之上增加运行器：检测状态 -> 调用对应 AI -> 写回 Markdown -> 推进下一回合。
