# 双休企业商品白名单标准 v1（Double-Rest Standard v1）

> 状态：草案（Doubao 起草，Round 20；待 GPT 复核后交用户确认）
> 定位：定义"什么叫合格双休"，作为白名单协议（`whitelist-protocol.md`）的判定依据。
> 配套：`specs/whitelist-protocol.md`｜会议：`meeting/doubao.md` Round 17-20

## 1. 双休定义（v1）

**合格双休**：在观察期内（默认 6 个月），企业作为劳动法意义上的用工主体：

1. 制度上实行标准工时制且每周休息日不少于 2 日（或特殊工时具备合法审批依据）；
2. 执行上绝大多数工作周实际满足双休；
3. 不存在被证实的常态化强制加班或违法压缩休息日行为。

适用范围：**法律主体（营业执照主体）**，不按品牌/产品线判定；集团下各子公司分别判定。一个主体通过认证，其目录条目才可获得 Verified 凭证。

## 2. 五维证据模型

每个维度由**证据类型 × 可信级别**构成。可信级别：

| 级别 | 定义 | 示例 |
|---|---|---|
| L1 | 官方/权威来源 | 社保系统记录、政府备案、劳动监察记录 |
| L2 | 企业主动申报 | 盖章规章制度、劳动合同范本、承诺函、考勤/排班样本 |
| L3 | 员工/第三方实名陈述 | 在职员工实名确认、离职员工陈述 |

**匿名陈述不构成独立证据**，仅作为举报线索进入复核。

| 维度 | 证据类型 | 最低要求（v1） |
|---|---|---|
| 制度面 | L2+：劳动合同范本/规章制度/员工手册；特殊工时审批文件 | 必须有明确的周休 2 日制度条款 |
| 执行面 | L2：考勤记录/排班表样本 | 观察期 ≥30 个工作周样本，双休周占比 ≥90% |
| 加班面 | L2/L3：加班审批记录、薪资单加班费项 | 无常态化强制周末加班证据 |
| 节假日面 | L2：放假通知、调休方案 | 法定节假日休足，调休不破坏双休 |
| 证据门槛 | 至少 1 个独立可核验来源（L1 或 L2） | 单一来源只能进入 Pending，不得直接 Verified |

## 3. Hard Fail 条件（硬性否决，任一命中即不通过，不参与评分）

1. **制度缺失**：无周休 2 日制度条款，或特殊工时无合法审批；
2. **常态化强制加班**：被证实连续 ≥4 周每周占用完整休息日加班，且未依法支付加班费/调休；
3. **违法调休**：被证实连续工作 >6 天未补休；
4. **材料造假**：任一申报证据被证伪；
5. **证据门槛不足**：无任何 L1/L2 独立可核验来源。

**原则：任何硬性否决条件优先于统计占比。** 占比通过不代表无 Hard Fail。

## 4. 状态机

| 状态 | 含义 | 可否上架 |
|---|---|---|
| Pending | 申请中/证据不足/单一来源待复核 | 否 |
| Verified | 通过全部维度且无 Hard Fail，认证有效期内 | **是（唯一可上架状态）** |
| Disputed | 被有效举报或出现反向证据，复核中 | 否 |
| Rejected | Hard Fail 命中或申诉失败 | 否 |
| Expired | 认证到期未复核 | 否（自动失效，非负面） |

转移规则：
- `Pending -> Verified`：补齐证据、通过全部维度
- `Verified -> Disputed`：有效举报或反向证据
- `Disputed -> Verified`：复核排除疑点（新 decision_id）
- `Disputed -> Rejected`：确证违规
- `Verified -> Expired`：到期未复核

## 5. 证据有效期与复核周期

- Verified 有效期：**6 个月**（v1），到期前 30 天可发起复核；
- 复核：重新提交执行面样本与制度面声明，生成新的 `evidence_set_id`；
- 过期未复核：状态转 Expired，从目录移除（可随时重新申请）。

## 6. 申诉与仲裁规则

- 企业对 Rejected / Disputed 可申诉，窗口 **14 天**；
- 申诉须提交新证据，进入复核队列；
- 复核：初筛（验证员）+ 仲裁小组（≥3 人，从验证员池抽取，执行回避）；
- 结果写入决策记录（`decision_id`），可追溯；
- MVP 无 Token，申诉不收费。

## 7. standard version 版本化规则

- 标准 = `{standard_version, rule_set}`；v1 初始冻结；
- 变更流程：**提案 → 公示 ≥14 天 → 生效**；
- 生效时：现存 Verified 企业进入新标准复核或过渡期评估，认证记录标注 `standard_version`；
- 治理主体（MVP）：维护者提议 + 公示 + 双方/社区确认；后续可逐步去中心化。

## 8. 隐私与链下证据要求

- **链上仅存哈希与元数据**：证据哈希（sha256）、证据类型、时间戳、状态、判定版本、decision_id；不含原件；
- 原件存受控链下存储，访问受控（企业可取回自身材料，审核员按需访问）；
- **最小化采集**：员工确认仅需在职状态 + 工作制声明，不采集薪资明细；
- **删除/更正/撤回**：企业可撤回并删除自身证据；撤回后状态转 Pending/Rejected；
- 对外展示仅限：状态 + 证据等级摘要；不展示原件。

---

*本文件为 v1 草案，参数（观察期/占比/有效期/公示期/窗口期）均可在复核中调整。*
